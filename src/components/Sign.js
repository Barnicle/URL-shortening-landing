import React from 'react';
import StyledButton from './styles/StyledButton'

const Sign = () => (
	  <div>
		  <button>Login</button>
		  <StyledButton radius={'10px'}>Sign Up</StyledButton>
	  </div>
	);


export default Sign;